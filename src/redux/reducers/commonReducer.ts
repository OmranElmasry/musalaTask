/** @format */

export const commonInitialState: any = {
    
}

const common = (state = commonInitialState, action: any) => {
    switch (action?.type) {
        default:
            return state
    }
}

export default common
